/*
const linkedList = (function () {

    const addName = (name) => {

        return {name}
    }





    return {}
})()

const node = (function () {
    
    const createNode = () => {
        const value = null
        const nextNode = null

        return {value, nextNode}
    }




    return {}
})()
*/

const linkedList = (list) => {

    

    return {list}
}


const createNode = () => {
    const value = null
    const nextNode = null

    const setValue = (input) => value = input
    const setNextNode = (input) => nextNode = input

    return {value, nextNode}
}